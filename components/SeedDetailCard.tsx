// Define component
SeedDetail(props) {
  // Initialize state for seed details
  state = {seed: null}

  // When component mounts or when props.seedId changes, fetch seed details from blockchain and update state

  // Render seed details
  render() {
    // If seed data is not yet loaded, display loading message
    // Else, display seed details based on state
  }
}
